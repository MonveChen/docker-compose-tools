version: '3'
services:

  openvpn:
    image: kylemanna/openvpn:2.4
    container_name: openvpn
    volumes:
      - ~/.data/openvpn/data:/etc/openvpn
    ports:
      - 1194:1194/udp
    cap_add:
      - NET_ADMIN
    privileged: true